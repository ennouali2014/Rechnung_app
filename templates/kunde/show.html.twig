{% extends 'base.html.twig' %}

{% block title %}Kunde{% endblock %}

{% block body %}
    <h1>Kunde</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kunde.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kunde.name }}</td>
            </tr>
            <tr>
                <th>Strasse</th>
                <td>{{ kunde.strasse }}</td>
            </tr>
            <tr>
                <th>Plz</th>
                <td>{{ kunde.plz }}</td>
            </tr>
            <tr>
                <th>Ort</th>
                <td>{{ kunde.ort }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_kunde_index') }}">back to list</a>

    <a href="{{ path('app_kunde_edit', {'id': kunde.id}) }}">edit</a>

    {{ include('kunde/_delete_form.html.twig') }}
{% endblock %}
