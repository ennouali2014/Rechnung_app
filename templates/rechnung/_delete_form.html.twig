<form method="post" action="{{ path('app_rechnung_delete', {'id': rechnung.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rechnung.id) }}">
    <button class="btn">Delete</button>
</form>
